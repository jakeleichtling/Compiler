void swap(double a[], int i, int j) {
	double temp = a[i];
	a[i] = a[j];
	a[j] = temp;
}

void main(void) {
	double a[10];
	int i;

	for(i = 0; i < 10; ++i) {
		a[i] = i;
	}

	for(i = 0; i < 9; ++i) {
		swap(a, i, i + 1);	
	}

	for(i = 0; i < 10; ++i) {
		print a[i];
	}
}