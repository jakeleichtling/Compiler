int f0(int x, double y[])
{
  int a;
  int b;
  int c;

  f0(5);

  c = a + b;

  do {
    ++b;
  } while (a>b);

  {
    ++b;
  }

  if (a) {
    b = c;
  } else if (b) {
    c = a;
  } else if (c) {
    a = b;
  }
  /*
  
}

int f1()
{
  int x;
  int y[x];
  x = y[0];
  y[1] = x;

  while (x) {
    int x;
    ++x;

    while (x) {
      int x;
      ++x;
      y[1] = x;
    }
  }

  while (x) {
    int x;
    ++x;
  }
}

int f2()
{
  int x;

  while (x) {
    int x;
    ++x;

    while (x) {
      int x;
      ++x;
    }
  }

  while (x) {
    int x;
    ++x;
  } */
}